{
  "name": "@proodos/tests",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p . --noEmit",
    "test": "jest --config src/config/jest.config.ts",
    "test:coverage": "jest --config src/config/jest.config.ts --coverage",
    "test:watch": "jest --config src/config/jest.config.ts --watch"
  }
}
